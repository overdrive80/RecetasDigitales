UI --> BBDD

<LinearLayout
    android:orientation="horizontal">

    <com.google.android.material.textfield.TextInputLayout
        android:hint="Horas"
        android:layout_weight="1">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etHoras"
            android:inputType="number" />
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.textfield.TextInputLayout
        android:hint="Minutos"
        android:layout_weight="1">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etMinutos"
            android:inputType="number" />
    </com.google.android.material.textfield.TextInputLayout>

</LinearLayout>

long horas = TextUtils.isEmpty(etHoras.getText()) ? 0 :
             Long.parseLong(etHoras.getText().toString());

long mins = TextUtils.isEmpty(etMinutos.getText()) ? 0 :
            Long.parseLong(etMinutos.getText().toString());

long tiempoTotalEnMinutos = (horas * 60) + mins;
receta.setTiempo(tiempoTotalEnMinutos);

Al crear/editar receta:

long total = horas * 60 + minutos;
if (total <= 0 || total > 24 * 60) { // más de 24h no tiene sentido práctico
    mostrarError("Introduce un tiempo válido (hasta 24 h)");
    return;
}
receta.setTiempo(total);

public static boolean esTiempoValido(long tiempo) {
    return tiempo > 0 && tiempo <= 1440; // hasta 24h
}

BBDD --> UI

long horas = tiempoTotalEnMinutos / 60;   // 1
long minutos = tiempoTotalEnMinutos % 60; // 35

binding.etHoras.setText(String.valueOf(horas));
binding.etMinutos.setText(String.valueOf(minutos));

    etHoras.setText(horas > 0 ? String.valueOf(horas) : "");
    etMinutos.setText(minutos > 0 ? String.valueOf(minutos) : "");

