En el repositorio se haría:

    @Transaction
    public void eliminarRecetaCompleta(Receta receta) {
        // Primero eliminamos dependencias (ingredientes y pasos)
        mIngredienteDAO.borrarIngredientesPorRecetaId(receta.getId());
        mPasoDAO.borrarPasosPorRecetaId(receta.getId());

        // Finalmente eliminamos la receta
        mRecetaDAO.borrarReceta(receta);
    }