<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppBaseTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryDark">@color/blue_200</item>
        <item name="colorAccent">@color/blue_200</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <!-- Colores secundarios -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <!-- Colores de texto -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Colores sobre contenedores -->
        <item name="colorOnPrimaryContainer">@color/blue_50</item>
        <!-- Configuración de la statusbar y fondo de la ventana -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- Colores de la toolbar -->
        <item name="android:textColorPrimary">?attr/colorOnPrimary</item>
        <item name="android:textColorSecondary">?attr/colorOnPrimary</item>
        <item name="android:colorControlNormal">?attr/colorOnPrimary</item>
        <item name="android:textColorPrimaryInverse">?attr/colorSecondary</item>
    </style>

    <style name="Theme.RecetasDigitales" parent="AppBaseTheme">
        <!-- Estilo específico para la acción de la barra -->
        <item name="actionBarStyle">@style/ActionBar</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowButton</item>
    </style>

    <style name="ActionBar" parent="Widget.Material3.ActionBar.Solid">
        <item name="background">?attr/colorPrimary</item>
        <item name="titleTextStyle">@style/ActionBarTexto</item>
        <item name="subtitleTextStyle">@style/ActionBarSubtitulo</item>
    </style>

    <style name="OverflowButton" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">?attr/colorOnPrimary</item>
    </style>

    <style name="ActionBarTexto" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ActionBarSubtitulo" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="EstiloTab" parent="Widget.Material3.TabLayout">
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Tema para SearchView -->
    <style name="ThemeOverlay.SearchView.DarkIcons" parent="ThemeOverlay.Material3.Light">
        <item name="android:colorControlNormal">@color/gray_700</item>
        <item name="android:textColorPrimary">@color/gray_800</item>
        <item name="android:textColorHint">@color/gray_400</item>
        <item name="android:elevation">0dp</item>
    </style>

    <!-- Tema para BottomSheet -->
    <style name="ThemeBottomSheet" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/EstiloBottomSheet</item>
    </style>
    <!-- Estilo del BottomSheet-->
    <style name="EstiloBottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="shapeAppearance">@style/ShapeAppearance.BordesBottomSheet</item>
        <item name="backgroundTint">@color/blue_25</item>
    </style>
    <!-- Forma de las esquinas -->
    <style name="ShapeAppearance.BordesBottomSheet" parent="ShapeAppearance.Material3.Corner.None">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">32dp</item>
        <item name="cornerSizeTopRight">32dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <style name="Theme.RecetasDigitales.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.RecetasDigitales.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RecetasDigitales.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>