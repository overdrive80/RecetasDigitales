<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estado arrastrando (activated=true) -->
    <item android:color="@color/dragCardView" android:state_pressed="true" />
    <item android:color="@color/dragCardView" android:state_activated="true" />

    <!-- Estado normal -->
    <item android:color="@color/fondoCardView" />

</selector>
