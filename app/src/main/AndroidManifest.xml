<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--    &lt;!&ndash; Devices running Android 12L (API level 32) or lower  &ndash;&gt;-->
    <!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />-->

    <!--    &lt;!&ndash; Devices running Android 13 (API level 33) or higher &ndash;&gt;-->
    <!--    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />-->
    <!--    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />-->

    <!--    &lt;!&ndash; To handle the reselection within the app on devices running Android 14-->
    <!--         or higher if your app targets Android 14 (API level 34) or higher.  &ndash;&gt;-->
    <!--    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.RecetasDigitales"
        tools:targetApi="33">
        <activity
            android:name=".view.crear_receta.CrearRecetaActivity"
            android:exported="false" />
        <activity
            android:name=".view.ver_receta.VerRecetaActivity"
            android:exported="false"
            android:label="@string/title_activity_ver_receta"
            android:theme="@style/Theme.RecetasDigitales.NoActionBar" />
        <activity
            android:name=".view.lista_recetas.RecetasActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>